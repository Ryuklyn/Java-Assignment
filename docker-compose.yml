version: '3.8'
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: griddb
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: secret
      PGTZ: UTC
    ports:
      - "5432:5432"

  redis:
    image: redis:7
    ports:
      - "6379:6379"